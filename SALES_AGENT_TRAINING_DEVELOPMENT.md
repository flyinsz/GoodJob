# 销售能力训练中心开发文档

## 1. 产品目标

把现有“一次性蒸馏”升级为持续训练系统。系统从真实 CRM 行为构造训练样本，经过采集、清洗、标注、策略训练、离线评测和主管审核后，生成可应用到 AI Agent 的版本化销售能力包。

首期训练对象是结构化销售策略、案例检索和 Agent 行为约束，不声称修改基础大模型参数。未来样本达到质量与数量门槛后，再通过独立适配器接入模型微调。

## 2. 核心闭环

```text
业务数据 -> 样本采集 -> 清洗脱敏 -> 自动/人工标注 -> 多轮策略训练
       -> 留出案例评测 -> 主管审核 -> 发布能力版本 -> Agent 应用
       -> 使用与结果反馈 -> 新版本再训练
```

已发布版本不可原地修改。重新训练必须生成新版本，旧版本保留并可重新应用，实现回退。

## 3. 数据范围

训练数据限定在操作者有权访问的业务员范围内：

- 客户、线索、客户跟进、线索活动、商机、商机事件、本人待办和日报。
- 正样本：成交、阶段推进、客户回复、按期完成下一动作。
- 负样本：丢单、长期停滞、无回复、逾期、缺少下一动作。
- 证据只保存 CRM 对象 ID、结构化特征和必要摘要；不向无权用户暴露原始客户隐私。

## 4. 训练任务状态

```text
queued -> collecting -> cleaning -> labeling -> training -> evaluating
       -> awaiting_review -> published
```

任务支持 `paused`、`failed`、`cancelled`。暂停和失败保留轮次、样本和事件，可恢复；发布后只能创建下一版本。

## 5. 训练产物

每个候选能力版本包含：

- 样本统计：来源数、有效数、淘汰数、正负样本和留出评测集。
- 优势模式：带证据数量和置信度的行为模式。
- 策略手册：线索进入、客户分层、开发信、跟进节奏、异议处理、商机推进和成交复盘。
- 禁止动作与风险边界。
- 待加强能力和训练建议。
- 离线评测：覆盖度、平衡性、证据可追溯、策略完整性、安全性和综合分。
- 成熟度：观察级、试用级、生产级、稳定级。

## 6. 发布门槛

- 观察级只能查看结果，不能发布。
- 试用级综合分不低于 65，可由主管发布并以辅助模式使用。
- 生产级综合分不低于 80，可作为团队正式能力版本。
- 安全性低于 90、存在越权结论或证据不可追溯时禁止发布。

## 7. Agent 接入

同一账号同一时间只启用一个能力版本。Agent 规划和客户守护读取当前版本的阶段策略、适用条件、禁止动作与证据。

应用模式首期包括：

- 影子模式：只记录建议，不改变 CRM。
- 辅助模式：生成计划，内部写入和外部发送继续确认。
- 自动内部模式：允许只读分析和受控创建站内待办，外部发送仍需确认。

每次应用记录版本、命中策略、是否产生待办和使用时间，作为下一轮训练的反馈证据。

## 8. 权限

- 业务员可训练并查看自己；主管、管理员可训练本团队成员。
- 只有主管、管理员和超级管理员可以发布团队能力版本。
- 训练任务、样本和事件按 `teamId`、`sourceUserId`、`createdBy` 过滤。
- 模型不可直接访问数据库，不可绕过现有 Agent 接口权限与确认机制。

## 9. API

- `POST /api/agent/sales-training`：创建异步训练任务。
- `GET /api/agent/sales-training`：列出可见训练任务。
- `GET /api/agent/sales-training/:id`：读取任务、样本、轮次和事件。
- `POST /api/agent/sales-training/:id/pause|resume|cancel`：控制任务。
- `PATCH /api/agent/sales-training/:id/samples/:sampleId`：人工修正标签、是否纳入和主管备注。
- `POST /api/agent/sales-training/:id/retrain`：基于当前样本创建下一版本。
- `POST /api/agent/sales-training/:id/publish`：通过门槛后发布能力包。
- 现有 `/api/agent/sales-distillation/:id/activate` 保留，用于应用或回退到任一已发布版本。

## 10. 界面

训练中心使用三部分布局：

- 左侧训练队列：业务员、版本、阶段、进度和状态。
- 中间训练过程：阶段轨道、当前动作、轮次流、清洗结果和训练事件。
- 右侧训练质量：样本结构、成熟度、离线评测、发布门槛和操作。

下方提供样本审阅和已发布能力卡片。卡片只在评测完成后出现，展示业务员、版本、擅长场景、成熟度、样本数、评测分和 Agent 应用状态。

## 11. 验收标准

- 创建训练后立即返回任务，不阻塞 HTTP 请求；后台分阶段运行并持久化。
- 刷新页面或重启后可恢复未完成任务。
- 用户能看到采集、清洗、训练和评测的实时事件，而不是点击后立即生成卡片。
- 样本不足时显示观察级并禁止发布。
- 人工修改标签后可创建新训练版本，旧发布版本不受影响。
- 发布、应用和回退均有权限隔离与审计记录。
- 无模型配置时仍能完成确定性训练；模型失败时不丢任务。
- 后端测试、前端测试和生产构建通过；1280、1600、1920、2560 桌面宽度无横向溢出。

