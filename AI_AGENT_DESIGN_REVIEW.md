# GoodJob CRM AI Agent 方案优化与三方审核

## 1. 目标

AI Agent 是 CRM 内置的业务协作入口，负责把用户的自然语言目标转换为可解释、可审核、可执行的 CRM 操作。

首期目标不是让模型获得任意后台权限，而是建立一条受控链路：

```text
用户目标 → Agent 分析 → 结构化行动计划 → 风险/权限检查 → 用户确认 → 白名单接口执行 → 实时结果
```

Agent 必须能够读取当前用户有权看到的客户、线索、待办、商机和搜客任务，并通过受控工具影响界面数据；不能直接执行任意 SQL、任意 HTTP 请求、任意 JavaScript 或绕过现有权限。

## 2. 产品边界

### 首期支持

- 客户、线索、商机、待办和搜客任务的只读检索。
- 客户/线索跟进摘要和下一步建议。
- 生成待办草稿、开发信草稿、客户背调提纲。
- 创建待办、记录跟进、更新客户健康度/分级等低风险 CRM 写操作。
- 发起搜客任务前展示完整计划、数据源、预计成本和影响范围。
- 发开发信、发送 WhatsApp、批量转客户、删除数据等高风险操作必须二次确认。
- 所有动作以事件流展示：计划、等待确认、执行中、完成、失败和回滚提示。

### 明确禁止

- 模型自行读取或写入数据库。
- 模型自行拼接接口 URL。
- 模型直接发送外部消息或邮件而跳过确认。
- 模型读取其他团队或其他业务员的私有数据。
- 模型声称已经完成网页背调、发送消息或成交判断，而实际没有对应事实证据。
- 通过前端隐藏按钮绕过后端权限。

## 3. Agent 权限等级

| 等级 | 默认动作 | 示例 |
|---|---|---|
| Read | 自动执行 | 搜索客户、查看待办、读取商机阶段 |
| Draft | 自动生成但不落库 | 开发信草稿、跟进建议、背调提纲 |
| Write | 需要用户确认 | 新建待办、记录跟进、修改客户分级 |
| External | 强制二次确认 | 发送邮件、发送 WhatsApp、调用付费数据源 |
| Destructive | 禁止由 Agent 直接执行 | 删除客户、清空数据、批量永久删除 |

## 4. 受控工具目录

首期工具固定为：

```text
crm.search_customers
crm.search_leads
crm.get_customer_overview
crm.get_pipeline_snapshot
crm.list_pending_todos
crm.create_todo
crm.record_customer_followup
crm.update_customer_profile
prospect.preview_search_plan
prospect.create_search_run
outreach.draft_development_email
```

工具参数使用 Zod 校验；工具执行前重新检查当前用户、团队、对象归属、幂等键和数据版本。工具返回结构化结果，前端只渲染结果，不执行模型返回的代码。

## 5. 三方审核

### 产品经理审核

**结论：通过，需分阶段交付。**

- Agent 入口应是一个独立工作台，不把复杂提示堆进客户页面。
- 默认先展示计划和影响范围，降低“AI 替用户做决定”的不确定性。
- 对业务员最有价值的闭环是：客户/线索检索 → 跟进建议 → 待办落库 → 结果回看。
- 搜客和外部触达必须把成本、数据源、收件对象和发送内容放在确认面板中。

### 高级外贸业务员审核

**结论：通过，必须强调业务上下文和证据。**

- 建议不能只有通用话术，要指出客户、商机阶段、最近一次跟进和下一动作。
- 开发信需要先生成草稿，业务员可编辑后再发送。
- 背调结果必须区分“已知事实、来源线索、待核实项”，不能把模型推测当成企业事实。
- 自动获客需要展示来源、清洗原因、重复判断和预计可用结果，不能只给一个数量。

### 后端安全工程师审核

**结论：有条件通过。**

- 所有工具必须在服务端注册，模型输出只能引用工具名和 JSON 参数。
- 每个写动作都要有短期签名计划、幂等键和审计事件。
- 不能把 API Key、客户隐私、原始模型响应或跨团队数据传入不必要的模型请求。
- 外部请求继续复用现有 `ai-http-security` 和 provider runtime 门禁。
- 首期允许内存态 Agent 计划快速验证，但生产版必须将 Agent 会话、动作和审计事件持久化到 MySQL。

## 6. 验收门槛

- 未配置模型时，Agent 工作台仍可打开，并明确显示“需要配置模型”，不能白屏。
- 模型输出格式错误时，系统显示可读错误，不执行任何写操作。
- 业务员不能读取其他团队客户；服务端拒绝伪造 `teamId`/`ownerId`。
- 同一个写动作重复提交只产生一次结果。
- 发送类动作必须出现确认界面，取消后不产生外部副作用。
- 浏览器 1280px 桌面视口无横向溢出；Agent 事件流可滚动，操作区固定可见。
